version: 2.1

executors:
  default:
    docker:
      - image: circleci/golang:1.13

aliases:
- &restore_cache
  restore_cache:
    key: go-mod-v1-{{ checksum "go.sum" }}
- &save_cache
  save_cache:
    key: go-mod-v1-{{ checksum "go.sum" }}
    paths:
    - "/go/pkg/mod"

commands:
  check-uncommitted:
    description: Check for uncommitted changes.
    steps:
      - run:
          name: Check for uncomitted changes using git porcelain command.
          command: |
            if [[ `git status --porcelain` ]]; then
              echo "ERROR: uncommitted changes"
              git status --porcelain
              exit 1
            fi

jobs:
  build:
    executor:
      name: default
    steps:
      - checkout
      - run: sudo mkdir -p $GOPATH/src/go.uber.org && sudo git clone https://github.com/uber-go/zap.git /root/go/src/go.uber.org/zap && sudo git clone https://github.com/uber-go/atomic /root/go/src/go.uber.org/atomic
      - run: sudo mkdir -p /root/go/src/go.uber.org && && sudo git clone https://github.com/uber-go/multierr.git /root/go/src/go.uber.org/multierr
      - run: sudo mkdir -p /root/go/src/hashicorp && sudo git clone https://github.com/hashicorp/go-plugin.git /root/go/src/hashicorp/go-plugin
      - run: sudo git clone https://github.com/hashicorp/go-hclog.git /root/go/src/hashicorp/go-hclog
      - run: which go
      - run: sudo /usr/local/go/bin/go get -u -v github.com/mattermost/mattermost-server/plugin
      - run: mkdir -p site/data
      - run: sudo /usr/local/go/bin/go run scripts/plugin-godocs.go > site/data/PluginGoDocs.json
      - run: go run scripts/plugin-manifest-docs.go > site/data/PluginManifestDocs.json
      # - run: sudo chmod -R 777 /go/
      - run: make dist
      - check-uncommitted
      - *save_cache

workflows:
  version: 2
  ci:
    jobs:
      - build
